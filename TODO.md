# TODO - Scan History responsive table fixes

- [ ] Analyze `app/templates/history.html` and identify CSS rules causing overflow/cutoff.
- [ ] Draft responsive, SOC-style CSS inside `history.html` to:
  - [ ] Enforce horizontal scroll for wide tables.
  - [ ] Prevent card/table overflow outside container.
  - [ ] Add filename truncation with tooltip support.
  - [ ] Ensure all columns remain visible on desktop/laptop.
  - [ ] Provide mobile-friendly behavior without breaking layout.
  - [ ] Remove/avoid duplicate/conflicting CSS (within the file).
- [ ] Replace `history.html` with fully updated, production-ready code (no backend changes).
- [ ] Explain fixes after code.

